// vercel.json - Vercel Deployment Configuration (This should be in the root directory)
{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "index.js" }
  ]
}
